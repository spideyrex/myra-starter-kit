<?php

namespace App\Services;

use App\Admin\Traits\SearchableQuery;
use App\Models\{{ model }};
use Illuminate\Contracts\Pagination\LengthAwarePaginator;
use Illuminate\Http\Request;

class {{ model }}Service
{
    use SearchableQuery;

    /**
     * `sortable` is declared explicitly rather than inheriting the searchable
     * list: the two whitelists answer different questions. Every LIKE is built
     * inside applySearchAndPaginate via Sql::orWhereLike.
     */
    public function list(Request $request): LengthAwarePaginator
    {
        return $this->applySearchAndPaginate(
            {{ model }}::query(),
            $request,
            searchable: ['name'],
            sortable: ['id', 'name', 'created_at'],
        );
    }

    public function create(array $data): {{ model }}
    {
        return {{ model }}::create($data);
    }

    public function update({{ model }} ${{ modelVariable }}, array $data): {{ model }}
    {
        ${{ modelVariable }}->update($data);

        return ${{ modelVariable }}->fresh();
    }

    public function delete({{ model }} ${{ modelVariable }}): void
    {
        ${{ modelVariable }}->delete();
    }
}
